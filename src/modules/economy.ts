import Logger from "./logger";

const log = new Logger();
const debug = true;

export default class Economy {

    /**
     * Load economy data
     *
     * @memberof Economy
     */
    public load(): void {

    }

}